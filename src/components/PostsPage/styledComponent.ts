import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const PostsPageWrapper = styled.div `${tw `flex justify-center items-center bg-blue-100`}`
export const PostsInnerWrapper = styled.div `${tw `w-5/6 bg-white rounded mt-10`}`
